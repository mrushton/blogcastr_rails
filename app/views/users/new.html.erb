<h2>Sign up</h2>

<% form_for @user, :url => {:controller => "/users"} do |form| %>
  <%= render :partial => '/users/form', :object => form %>
  <%= form.submit 'Sign up', :disable_with => 'Please wait...' %>
<% end %>
