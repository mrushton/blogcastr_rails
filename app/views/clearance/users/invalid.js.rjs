page << "var val = jQuery('#blogcastr_user_username').val();"
page << "if (val == \"" + @username + "\") {"
page << "  jQuery('#pick-username-container').removeClass('loading').addClass('invalid')"
page << "}"
