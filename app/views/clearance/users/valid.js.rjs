page << "var val = jQuery('#blogcastr_user_username').val();"
page << "if (val == \"" + @username + "\")"
page << "{"
page << "jQuery('#pick-username-container').removeClass().addClass('valid')"
page << "}"
