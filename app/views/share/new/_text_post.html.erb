<%- user = text_post.user -%>
<li class="post" id="TextPost:<%= text_post.id -%>">
  <div>
    <div class="post-header">
      <span class="timestamp-in-words" timestamp="<%= text_post.created_at.to_i.to_s -%>" ><%= timestamp_in_words_helper text_post.created_at -%></span>
    </div>
    <div class="post-body">
      <div class="clearfix">
        <%= link_to image_tag(user.setting.avatar(:small), :class => "small-avatar"), profile_path(:username => user.username) -%>
        <div class="post-info">
          <div class="username">
            <%= link_to user.username, profile_path(:username => user.username), :class => "username" -%>
          </div>
          <p class="text">
            <%= text_post.text -%>
          </p>
        </div>
      </div>
    </div>
  </div>
</li>
