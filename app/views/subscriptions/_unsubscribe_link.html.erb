<%= button_to_remote "Unsubscribe", :url => (subscription_path :id => @subscription.id), :method => "delete", :failure => "alert('Error: Unable to unsubscribe from #{@profile_user.name}. Failed communicating with server.');" %>
